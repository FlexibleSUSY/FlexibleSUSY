#!/bin/sh

# directory of this script
BASEDIR=$(dirname $0)

cp ${BASEDIR}/mynewobservable.* ${BASEDIR}/../../templates/observables

META=${BASEDIR}/../../meta/Observables/MyNewObservable
mkdir ${META}
cp ${BASEDIR}/FlexibleSUSY.m ${META}
cp ${BASEDIR}/Observables.m ${META}
cp ${BASEDIR}/WriteOut.m ${META}
